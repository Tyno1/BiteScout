import { ScrollView, Text, View } from "react-native";

export default function DietaryPreferences() {
	return (
		<ScrollView className="flex-1 bg-background">
			<View className="p-4">
				<Text className="text-foreground">
					Content for Dietary Preferences screen.
				</Text>
			</View>
		</ScrollView>
	);
}
