// Mock for use-key-match package
export const useKeyMatch = (callbacks) => {
  // Mock implementation - does nothing in tests
  // The actual keyboard handling will be tested through user interactions
};
