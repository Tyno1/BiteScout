const Dashboard = () => {
  return (
    <div className="w-full min-h-[100vh] bg-orange">
      <button className="bg-red p-2 m-2">Call APi</button>
      Dash Home
      {/* <p>{message && message.map((mes: any) => <div key={mes}>{mes}</div>)}</p> */}
    </div>
  );
};
export default Dashboard;
